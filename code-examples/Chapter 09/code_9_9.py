from collections import OrderedDict

# Creating an OrderedDict
od = OrderedDict()

# Adding items to the OrderedDict
od['a'] = 1
od['b'] = 2
od['c'] = 3

# Printing keys in the order they were added
print(list(od.keys()))
